const ShowPeiChart = ({data}) => {

  const dataArray = Array.isArray(data) ? data : [data];
  const size=dataArray.length;
  console.log(dataArray)
  // console.log(size)
  const initial=12;

  return (
    <div>
 
    
    </div>
  );
};

export default ShowPeiChart;